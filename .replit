run = "pip install -r server/requirements.txt --quiet && python3 -m uvicorn server.main:app --host 0.0.0.0 --port ${PORT:-8000}"
entrypoint = "server/main.py"

[env]
PYTHONUNBUFFERED = "1"

[nix]
channel = "stable-24_05"
